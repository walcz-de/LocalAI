# ROCm build: torch + vllm are installed by install.sh via wheels.vllm.ai/rocm/0.19.0/rocm721/
# (Python 3.12 required — pre-built ROCm vllm wheels are cp312-only)
# This file is intentionally left without torch/vllm to avoid pulling CUDA wheels from PyPI.
